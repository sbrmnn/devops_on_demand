<!DOCTYPE html>
<html>
<head>
  <%= analytics_init if GoogleAnalytics.valid_tracker? %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>DevopsOnDemand</title>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body class=<%= "#{controller.controller_name}_#{controller.action_name}" %> >

<% if render_navbar_and_footer? %>
  <nav class="navbar navbar-light bg-light navbar-expand-lg">
    <div class="container px-3 py-2">
      <a href="/" class="navbar-brand d-flex align-items-center">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path><circle cx="12" cy="13" r="4"></circle></svg>
        <strong>YumFog</strong>
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <% if user_signed_in? %>
          <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
              <button class="nav-link btn btn-outline-light dashboard-btn" data-target="#tabs-dashboard"><i class="fa fa-user-circle mr-1"></i><%= t("dashboard") %></button>
            </li>
            <li class="nav-item">
              <button class="nav-link dashboard-btn" data-target="#tabs-messages"><i class="fa fa-comments mr-1"></i><%= t("messages") %></button>
            </li>
            <li class="nav-item">
              <button class="nav-link dashboard-btn" data-target="#tabs-find-freelancers"><i class="fa fa-search mr-1"></i><%= t('find_freelancers') %></button>
            </li>
            <li class="nav-item">
              <button class="nav-link dashboard-btn" data-target="#tabs-settings"><i class="fa mr-1 fa-cog"></i> <%= t('settings') %></button>
            </li>
          </ul>
        <% end %>
        <%= render 'login_logout' %>
      </div>
    </div>
  </nav>
<% end %>
<%= yield %>
</body>
</html>
