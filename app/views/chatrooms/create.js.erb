if ($('.message_box_product_<%= @product.id %> > .chat-box').length === 0){
    $('.message_box_product_<%= @product.id %>').append("<%= escape_javascript render(partial: 'chatbox_id', locals: { cp:  ChatroomPresenter.new(@chatroom) }) %>")
}

renderChatRoomMessages(<%= @chatroom.id %>)
subscribeToRoom(<%= @chatroom.id %>)